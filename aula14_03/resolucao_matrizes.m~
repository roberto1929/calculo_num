%Método para resolução de sistemas
%linha 2 = linha 2 - linha 1 x A;
%pivo => a = a21/ a11

mat_x = [1 4 2 ;-1 0 3;1 -2 1]; 

vet_res = [17; 11; 1];
sol = zeros(size(vet_res);

%pivo% 



while(1)
    a = mat_x(1:1)/ mat_y(1:1);
    lp = vet_res(1) - (vet_res(2)*a);
    lp
end